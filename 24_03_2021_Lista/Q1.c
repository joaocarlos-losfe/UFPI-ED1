/*
	João Carlos de Sousa Fé - 26/03/2021
	Estrutura de Dados I
*/

/*

1) Em cada uma das afirmações a seguir marque V para verdadeiro e F para Falso e justifique quando falso.

a) ( V ) Os parâmetros de uma função podem ser passados por valor, se um parâmetro é passado por valor 
o seu conteúdo não é alterado dentro da função.

b) ( V ) Um parâmetro de uma função está sendo passado por referência quando ao chamar a 
função passa-se o * junto a variável referente aquele parâmetro, para que o mesmo possa ser alterado.

c) ( F ) A ordem dos parâmetros em uma função depende dos valores que se deseja passar 
na chamada da função.

d) ( V ) Uma chamada recursiva acontece quando uma função chama a si mesma, e neste momento 
se um parâmetro for passado por referência deve-se colocar o &.

e) ( V ) A chamada recursiva pode tornar um laço infinito se a variável de retorno da função, 
for sempre reiniciada, dentro da função.

f) ( V ) Um recursão com pendência só acontece quando se chama a função mais de uma vez.

g) ( F ) Uma função recursiva não pode retornar valor pela função, ou seja, usar return, 
pois assim ela sempre retornará o primeiro valor.

h) ( F ) Vetor é um conjunto de dados do mesmo tipo, a qual é alocado um espaço todas as 
vezes que se necessitar colocar um novo valor.

i) ( V ) O acesso ao vetor é sequencial, ou seja, para acessar o elemento da posição 
10 se faz necessário acessar todos os anteriores primeiro.

j) ( V ) Uma fila estática é uma estrutura que permite que o elemento sempre fique 
no mesmo lugar, como o próprio nome diz, estático.

k) ( F ) Uma pilha estática pode conter vários tipos de dados, desempilhando sempre 
o do topo da pilha e inserindo no topo.

*/